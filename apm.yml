name: working-title-immich-clip-expose
version: 1.0.0
description: APM project for working-title-immich-clip-expose
author: Daniel
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: agent-skills, antigravity, claude, codex, copilot, cursor, gemini, grok-build, kiro, opencode, windsurf
targets:
  - opencode
dependencies:
  apm:
    - dhcgn/personal-dev-skills/profiles/dev-go
  mcp: []
includes: auto
scripts: {}
